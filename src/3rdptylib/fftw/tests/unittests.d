import fftw3;

int
main()
{
	/* Compiling the fftw3 library with both -unittest and -version=fftwunittests
	   will run all the unittests verifying the right linkage for every function */

	return 0;
}
